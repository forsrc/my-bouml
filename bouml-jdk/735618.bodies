class PrimeNumberSequenceGenerator
!!!5069570.java!!!	generateSequence(inout arrayToFill : int [[]]) : void
        Random r = new Random();
        for (int i = 0; i < arrayToFill.length; i++) {
            arrayToFill[i] = PRIMES[r.nextInt(PRIMES.length)];
        }
