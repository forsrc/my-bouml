class LCount
!!!4309634.java!!!	lookup(inout evtName : String) : LCount
        LCount lc=(LCount)lCounts.get(evtName);
        if(lc==null)
            lCounts.put(evtName,(lc=new LCount()));
        return lc;
