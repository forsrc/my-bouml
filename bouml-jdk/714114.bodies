class XIntPool
!!!4846082.java!!!	getXInt(in value : int) : XInt
        if (value >= 0 && value < fXIntPool.length)
            return fXIntPool[value];
        else
            return new XInt(value);
