class LastMatch
!!!9455106.java!!!	match(inout matcher : Matcher, in i : int, inout seq : CharSequence) : boolean
            if (i != matcher.oldLast)
                return false;
            return next.match(matcher, i, seq);
