class Container
!!!4804610.java!!!	getContainer(in size : int) : Container
        if (size > THRESHOLD)
            return new LargeContainer(size);
        else
            return new SmallContainer(size);
